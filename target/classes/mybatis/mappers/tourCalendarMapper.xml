<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
   <mapper namespace="tourCalendarMapper">
   	<select id="calendarSelect" parameterType="TourCalendarVO" resultType="TourCalendarVO" >
   		select * from tour where tourNum = #{tourNum}
   	</select>
   
   
   </mapper>